server:
  port: 8081

spring:
  application:
    name: springHW
  datasource:
    url: jdbc:mysql://localhost:3306/guestbooks
    username: likelion
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true