server:
  port: 8081

spring:
  application:
    name: springHW
  datasource:
    url: jdbc:mysql://localhost:3306/guestbooks
    username: likelion
    password: Likelion14th
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true